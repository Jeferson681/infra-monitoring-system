import pytest

# This module was moved to tests/main/ during test tree reorganization.
# Keep a module-level skip to avoid duplicate test collection while retaining history.
pytest.skip("moved to tests/main/", allow_module_level=True)
