import pytest


# This module was moved to tests/integration/ during test tree reorganization.
# Keep a module-level skip here to avoid duplicate test collection while retaining history.
pytest.skip("moved to tests/integration/", allow_module_level=True)
