name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest-cov codecov

      - name: Run tests with coverage
        run: |
          pytest --maxfail=1 --disable-warnings --cov=src --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          fail_ci_if_error: true

      - name: Run lint (ruff, flake8, black)
        run: |
          ruff src/ tests/
          flake8 src/ tests/
          black --check src/ tests/

      - name: Run GG Guardian
        uses: GuardRailsio/guardrails-action@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_error: true
          scan_paths: src/ tests/
          skip_paths: ''
          verbose: true
          output_format: summary
          language: python

      - name: Run GG Guardian
        uses: GuardRailsio/guardrails-action@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_error: true
          scan_paths: src/ tests/
          skip_paths: ''
          verbose: true
          output_format: summary
          language: python

      - name: Run Bandit (security)
        run: |
          bandit -r src/ --skip B404,B603

  build-windows:
    runs-on: windows-latest
    needs: build-ubuntu
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest-cov codecov

      - name: Run tests with coverage
        run: |
          pytest --maxfail=1 --disable-warnings --cov=src --cov-report=xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage.xml
          fail_ci_if_error: true

      - name: Run lint (ruff, flake8, black)
        run: |
          ruff src/ tests/
          flake8 src/ tests/
          black --check src/ tests/

      - name: Run GG Guardian
        uses: GuardRailsio/guardrails-action@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_error: true
          scan_paths: src/ tests/
          skip_paths: ''
          verbose: true
          output_format: summary
          language: python

      - name: Run GG Guardian
        uses: GuardRailsio/guardrails-action@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          fail_on_error: true
          scan_paths: src/ tests/
          skip_paths: ''
          verbose: true
          output_format: summary
          language: python

      - name: Run Bandit (security)
        run: |
          bandit -r src/ --skip B404,B603
