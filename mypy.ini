[mypy]
python_version = 3.13
strict = False
files = src

# Adjust mypy behavior to be permissive for this repo's current state.
# You can tighten rules later (e.g., enable disallow_untyped_defs) as types are added.

[mypy-psutil]
ignore_missing_imports = True
